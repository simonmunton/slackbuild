--- matplotlib-3.10.9/pyproject.toml.orig	2026-04-23 23:56:12.000000000 +0100
+++ matplotlib-3.10.9/pyproject.toml	2026-04-27 22:32:36.267551654 +0100
@@ -48,9 +48,9 @@
 [project.optional-dependencies]
 # Should be a copy of the build dependencies below.
 dev = [
-    "meson-python>=0.13.1,<0.17.0",
+    "meson-python>=0.13.1,<0.20.0",
     "pybind11>=2.13.2,!=2.13.3",
-    "setuptools_scm>=7,<10",
+    "setuptools_scm>=7,<11",
     # Not required by us but setuptools_scm without a version, cso _if_
     # installed, then setuptools_scm 8 requires at least this version.
     # Unfortunately, we can't do a sort of minimum-if-instaled dependency, so
@@ -72,9 +72,9 @@
 build-backend = "mesonpy"
 # Also keep in sync with optional dependencies above.
 requires = [
-    "meson-python>=0.13.1,<0.17.0",
+    "meson-python>=0.13.1,<0.20",
     "pybind11>=2.13.2,!=2.13.3",
-    "setuptools_scm>=7,<10",
+    "setuptools_scm>=7,<11",
 ]
 
 [tool.meson-python.args]
