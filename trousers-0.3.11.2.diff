--- trousers-0.3.11.2/src/tspi/rpc/hosttable.c.orig	2013-07-12 17:27:37.000000000 +0100
+++ trousers-0.3.11.2/src/tspi/rpc/hosttable.c	2014-04-08 21:56:07.077736145 +0100
@@ -51,6 +51,9 @@
 {
 	struct host_table_entry *hte, *next = NULL;
 
+	if (ht == NULL)
+		return;
+
 	MUTEX_LOCK(ht->lock);
 
 	for (hte = ht->entries; hte; hte = next) {
