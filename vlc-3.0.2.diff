--- vlc-3.0.2/configure.orig	2018-04-23 22:07:40.485778334 +0100
+++ vlc-3.0.2/configure	2018-05-12 16:16:14.486293448 +0100
@@ -54816,12 +54816,12 @@
     pkg_cv_VSXU_CFLAGS="$VSXU_CFLAGS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"libvsxu\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "libvsxu") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"libvsx\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "libvsx") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_VSXU_CFLAGS=`$PKG_CONFIG --cflags "libvsxu" 2>/dev/null`
+  pkg_cv_VSXU_CFLAGS=`$PKG_CONFIG --cflags "libvsx" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -54833,12 +54833,12 @@
     pkg_cv_VSXU_LIBS="$VSXU_LIBS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"libvsxu\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "libvsxu") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"libvsx\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "libvsx") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_VSXU_LIBS=`$PKG_CONFIG --libs "libvsxu" 2>/dev/null`
+  pkg_cv_VSXU_LIBS=`$PKG_CONFIG --libs "libvsx" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -54859,9 +54859,9 @@
         _pkg_short_errors_supported=no
 fi
         if test $_pkg_short_errors_supported = yes; then
-	        VSXU_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "libvsxu" 2>&1`
+	        VSXU_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "libvsx" 2>&1`
         else
-	        VSXU_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "libvsxu" 2>&1`
+	        VSXU_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "libvsx" 2>&1`
         fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$VSXU_PKG_ERRORS" >&5
