--- inkscape-1.4.3/src/extension/internal/pdfinput/poppler-cairo-font-engine.cpp.orig	2025-12-26 00:35:50.000000000 +0000
+++ inkscape-1.4.3/src/extension/internal/pdfinput/poppler-cairo-font-engine.cpp	2026-05-04 11:21:35.630509693 +0100
@@ -313,7 +313,11 @@
 #else
     GfxFontLoc *fontLoc;
 #endif
+#if POPPLER_CHECK_VERSION(26, 5, 0)
+    std::array<const char*, 256> enc;
+#else
     char **enc;
+#endif
     const char *name;
 #if POPPLER_CHECK_VERSION(25, 7, 0)
     std::unique_ptr<FoFiType1C> ff1c;
@@ -677,7 +681,7 @@
 #endif
 
     std::vector<int> codeToGID;
-    char *name;
+    const char *name;
 
     Dict *charProcs = gfx8bit->getCharProcs();
     Ref ref = *gfxFont->getID();
@@ -694,7 +698,11 @@
 
     cairo_font_face_set_user_data(font_face, &type3_font_key, (void *)info, _free_type3_font_info);
 
+#if POPPLER_CHECK_VERSION(26, 5, 0)
+    std::array<const char*, 256> enc = gfx8bit->getEncoding();
+#else
     char **enc = gfx8bit->getEncoding();
+#endif
     codeToGID.resize(256);
     for (int i = 0; i < 256; ++i) {
         codeToGID[i] = 0;
