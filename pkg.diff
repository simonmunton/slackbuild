diff -ur w/ASB.build h/ASB.build
--- w/ASB.build	2005-05-04 08:45:22.000000000 +0100
+++ h/ASB.build	2005-05-04 08:45:22.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.4
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cd $PKGNAME-$VERSION
 aclocal
 autoconf
@@ -103,4 +106,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/Imaging.build h/Imaging.build
--- w/Imaging.build	2005-09-16 12:06:31.000000000 +0100
+++ h/Imaging.build	2005-09-16 12:06:31.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=Imaging
 VERSION=1.1.5
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/i
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/Numeric.build h/Numeric.build
--- w/Numeric.build	2005-09-16 10:58:12.000000000 +0100
+++ h/Numeric.build	2005-09-16 10:58:12.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=Numeric
 VERSION=24.0b2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/n
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -86,4 +97,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/PyOpenGL.build h/PyOpenGL.build
--- w/PyOpenGL.build	2005-03-14 22:32:32.000000000 +0000
+++ h/PyOpenGL.build	2005-03-14 22:32:32.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=PyOpenGL
 VERSION=2.0.2.01
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -86,4 +97,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/SDL_sound.build h/SDL_sound.build
--- w/SDL_sound.build	2004-07-28 10:57:58.000000000 +0100
+++ h/SDL_sound.build	2004-07-28 10:57:58.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=SDL_sound
 VERSION=1.0.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -72,4 +83,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/SDLinvaders.build h/SDLinvaders.build
--- w/SDLinvaders.build	2005-04-18 11:09:56.000000000 +0100
+++ h/SDLinvaders.build	2005-04-18 11:09:56.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=SDLInvaders
 VERSION=0.8.0
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -93,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/SMC.build h/SMC.build
--- w/SMC.build	2005-07-13 13:26:13.000000000 +0100
+++ h/SMC.build	2005-07-13 13:26:13.000000000 +0100
@@ -8,6 +8,8 @@
 VERSIONEXTRA=_source
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s/SMC
 PKG=/tmp/package-$PKGNAME
@@ -130,4 +132,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/a52dec.build h/a52dec.build
--- w/a52dec.build	2004-07-23 21:40:33.000000000 +0100
+++ h/a52dec.build	2004-07-23 21:40:33.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=a52dec
 VERSION=0.7.4
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -62,4 +73,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/acpitool.build h/acpitool.build
--- w/acpitool.build	2005-03-25 20:41:33.000000000 +0000
+++ h/acpitool.build	2005-03-25 20:41:33.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=acpitool
 VERSION=0.2.7
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/akregator.build h/akregator.build
--- w/akregator.build	2004-09-03 10:19:47.000000000 +0100
+++ h/akregator.build	2004-09-03 10:19:47.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=akregator
 VERSION=1.0_beta6
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -61,4 +72,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/alsa-driver.build h/alsa-driver.build
--- w/alsa-driver.build	2005-06-07 20:11:27.000000000 +0100
+++ h/alsa-driver.build	2005-06-07 20:11:27.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=alsa-driver
 VERSION=1.0.9b
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a/alsa
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/alsa-lib.build h/alsa-lib.build
--- w/alsa-lib.build	2005-06-03 12:35:29.000000000 +0100
+++ h/alsa-lib.build	2005-06-03 12:35:29.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.0.9
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a/alsa
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -101,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/alsa-oss.build h/alsa-oss.build
--- w/alsa-oss.build	2005-06-03 13:05:16.000000000 +0100
+++ h/alsa-oss.build	2005-06-03 13:05:16.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.0.9
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a/alsa
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/alsa-plugins.build h/alsa-plugins.build
--- w/alsa-plugins.build	2005-06-03 12:46:51.000000000 +0100
+++ h/alsa-plugins.build	2005-06-03 12:46:51.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.0.9
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a/alsa
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/alsa-utils.build h/alsa-utils.build
--- w/alsa-utils.build	2005-06-03 20:28:58.000000000 +0100
+++ h/alsa-utils.build	2005-06-03 20:28:58.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.0.9a
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a/alsa
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 ( cd $PKGNAME-$VERSION
   zcat $CWD/alsa-utils.alsaconf.diff.gz | patch -p1
 )
@@ -123,4 +126,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/apache-ant.build h/apache-ant.build
--- w/apache-ant.build	2005-07-27 14:03:59.000000000 +0100
+++ h/apache-ant.build	2005-07-27 14:03:59.000000000 +0100
@@ -8,6 +8,8 @@
 VERSIONEXTRA=-src
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
@@ -112,4 +114,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/apcupsd.build h/apcupsd.build
--- w/apcupsd.build	2005-08-03 10:15:47.000000000 +0100
+++ h/apcupsd.build	2005-08-03 10:15:47.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=3.10.18
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
  patch -p0 < $CWD/apcupsd.diff
 )
@@ -126,4 +129,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/asc.build h/asc.build
--- w/asc.build	2004-11-18 19:49:09.000000000 +0000
+++ h/asc.build	2004-11-18 19:49:09.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=asc
 VERSION=1.15.1.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a/asc
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-source-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-source-$VERSION$PKG_EXT
 cp $PKGDIR/*.mp3 $PKGNAME-$VERSION/data/music
 fi
 
@@ -84,4 +95,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/atlas.build h/atlas.build
--- w/atlas.build	2004-08-19 21:28:58.000000000 +0100
+++ h/atlas.build	2005-08-13 21:41:26.000000000 +0100
@@ -2,71 +2,35 @@
 #
 CWD=`pwd`
 PKGNAME=Atlas
-VERSION=0.2.2
+VERSION=0.3.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
-BUILD=2
+BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
-PKGDIR=~/tmp/new/a
+PKGDIR=~/tmp/new/f/Flightgear
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
 
-cat << EOF | patch -p1
-diff -ur Atlas-0.2.2/src/Map.cxx Atlas-0.2.2.x/src/Map.cxx
---- Atlas-0.2.2/src/Map.cxx	2001-09-22 16:57:35.000000000 +0100
-+++ Atlas-0.2.2.x/src/Map.cxx	2004-08-18 21:53:58.000000000 +0100
-@@ -311,7 +311,7 @@
-     strcpy(outname, outp);
-     opathl = strlen(outname);
-     strcpy( scenerypath, mapobj.getFGRoot() );
--    strcat( scenerypath, "/Scenery/" );
-+    strcat( scenerypath, "/Scenery/Terrain/" );
-     spathl = strlen(scenerypath);
-     
-     if ( (dir1 = opendir(scenerypath)) == NULL ) {
-diff -ur Atlas-0.2.2/src/MapMaker.cxx Atlas-0.2.2.x/src/MapMaker.cxx
---- Atlas-0.2.2/src/MapMaker.cxx	2003-01-06 21:02:37.000000000 +0000
-+++ Atlas-0.2.2.x/src/MapMaker.cxx	2004-08-18 21:58:48.000000000 +0100
-@@ -173,7 +173,7 @@
-   char *subdir = new char[strlen(fg_root) + 512];  /* 512 should be *very* 
-                                                       safe (too safe?) */
-   strcpy( subdir, fg_root );
--  strcat( subdir, "/Scenery/" );
-+  strcat( subdir, "/Scenery/Terrain/" );
-   int slen = strlen( subdir );       // index to where we should append
- 
-   if (getVerbose());
-@@ -691,7 +691,7 @@
-     printf("%s:  ", path + plen);
- 
-   if ((dir = opendir(path)) == NULL) {
--    if (getVerbose()) printf("\n");
-+    if (getVerbose()) printf(" -\n");
-     return 0;
-   }
- 
-diff -ur Atlas-0.2.2/src/MapPS.cxx Atlas-0.2.2.x/src/MapPS.cxx
---- Atlas-0.2.2/src/MapPS.cxx	2001-07-04 22:35:13.000000000 +0100
-+++ Atlas-0.2.2.x/src/MapPS.cxx	2004-08-18 21:54:24.000000000 +0100
-@@ -157,7 +157,7 @@
-     strcpy(outname, outp);
-     opathl = strlen(outname);
-     strcpy( scenerypath, mapobj.getFGRoot() );
--    strcat( scenerypath, "/Scenery/" );
-+    strcat( scenerypath, "/Scenery/Terrain/" );
-     spathl = strlen(scenerypath);
-     
-     if ( (dir1 = opendir(scenerypath)) == NULL ) {
-EOF
 
 #chown -R root.root .
 find . -perm 666 -exec chmod 644 {} \;
@@ -120,4 +84,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/audacity.build h/audacity.build
--- w/audacity.build	2004-11-19 22:26:27.000000000 +0000
+++ h/audacity.build	2004-11-19 22:26:27.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=audacity
 VERSION=1.2.3
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-src-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-src-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-src-$VERSION$PKG_EXT
 
 cd $PKGNAME-src-$VERSION/lib-src/portaudio-v19
 aclocal
@@ -75,4 +86,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/autofig.build h/autofig.build
--- w/autofig.build	2005-07-29 13:56:48.000000000 +0100
+++ h/autofig.build	2005-07-29 13:56:48.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/autopano-sift.build h/autopano-sift.build
--- w/autopano-sift.build	2005-09-09 15:06:51.000000000 +0100
+++ h/autopano-sift.build	2005-09-09 15:06:51.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -105,4 +108,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/autotrace.build h/autotrace.build
--- w/autotrace.build	2005-07-29 15:07:22.000000000 +0100
+++ h/autotrace.build	2005-07-29 15:07:22.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.31.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
 #./autogen.sh
 patch -p0 < $CWD/autotrace-configure.diff
@@ -102,4 +105,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/avidemux.build h/avidemux.build
--- w/avidemux.build	2005-05-29 08:33:31.000000000 +0100
+++ h/avidemux.build	2005-09-15 19:05:23.000000000 +0100
@@ -2,20 +2,31 @@
 #
 CWD=`pwd`
 PKGNAME=avidemux
-VERSION=2.0.40
+VERSION=2.0.42
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -58,4 +69,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/avifile.build h/avifile.build
--- w/avifile.build	2005-01-07 10:50:14.000000000 +0000
+++ h/avifile.build	2005-01-07 10:50:14.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=avifile
 VERSION=0.7.38
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/a/avifile
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-0.7-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-0.7-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-0.7-$VERSION$PKG_EXT
 fi
 
 cd $PKGNAME-0.7-$VERSION
@@ -67,4 +78,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/barcode.build h/barcode.build
--- w/barcode.build	2005-08-05 08:40:03.000000000 +0100
+++ h/barcode.build	2005-08-04 20:24:02.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.98
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/gnu/barcode
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/base64-josefsson.build h/base64-josefsson.build
--- w/base64-josefsson.build	2005-09-20 10:58:16.000000000 +0100
+++ h/base64-josefsson.build	2005-09-20 10:58:16.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/b
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv base64-$VERSION $PKGNAME-$VERSION
 fi
 
@@ -99,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/base64-walker.build h/base64-walker.build
--- w/base64-walker.build	2005-09-20 10:56:58.000000000 +0100
+++ h/base64-walker.build	2005-09-20 10:56:58.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/b
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-walker-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME-$VERSION $PKGNAME-walker-$VERSION
 fi
 
@@ -101,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/bluefish.build h/bluefish.build
--- w/bluefish.build	2005-07-18 09:24:29.000000000 +0100
+++ h/bluefish.build	2005-07-18 09:24:29.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.0.2
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/b
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -104,4 +107,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/bogofilter.build h/bogofilter.build
--- w/bogofilter.build	2005-07-26 22:06:49.000000000 +0100
+++ h/bogofilter.build	2005-07-26 22:06:49.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=0.95.2
+VERSIONEXTRA=
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
@@ -14,9 +15,9 @@
 PKGDIR=~/tmp/new/b
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -26,7 +27,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
diff -ur w/bonnie++.build h/bonnie++.build
--- w/bonnie++.build	2004-09-14 14:26:52.000000000 +0100
+++ h/bonnie++.build	2004-09-14 14:26:52.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=bonnie++
 VERSION=1.03a
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/b/benchmark
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tgz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION.tgz
 fi
 
 cd $PKGNAME-$VERSION
@@ -62,4 +73,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/boost.build h/boost.build
--- w/boost.build	2005-02-21 22:35:52.000000000 +0000
+++ h/boost.build	2005-02-21 22:35:52.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=boost
 VERSION=1_32_0
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=2
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/b
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d ${PKGNAME}_$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/${PKGNAME}_$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/${PKGNAME}_$VERSION$VERSIONEXTRA$PKG_EXT
 (cd ${PKGNAME}_$VERSION; patch -p0 < $CWD/boost-base.patch; patch -p0 < $CWD/boost-gcc-tools.patch)
 fi
 
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/cairo.build h/cairo.build
--- w/cairo.build	2005-09-09 12:27:59.000000000 +0100
+++ h/cairo.build	2005-09-09 12:27:59.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.0.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -100,4 +103,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/calltree-2.3.diff h/calltree-2.3.diff
--- w/calltree-2.3.diff	2005-09-21 14:23:13.000000000 +0100
+++ h/calltree-2.3.diff	2004-06-08 09:41:07.000000000 +0100
@@ -1,3 +1,15 @@
+diff -ur calltree-2.3.orig/DEFAULTS/Defaults.linux calltree-2.3/DEFAULTS/Defaults.linux
+--- calltree-2.3.orig/DEFAULTS/Defaults.linux	2003-02-16 00:01:48.000000000 +0000
++++ calltree-2.3/DEFAULTS/Defaults.linux	2004-05-20 15:18:29.000000000 +0100
+@@ -27,7 +27,7 @@
+ # Installation config stuff
+ #
+ ###########################################################################
+-INS_BASE=	/opt/schily
++INS_BASE=	/usr
+ INS_KBASE=	/
+ #
+ DEFUMASK=	002
 diff -ur calltree-2.3.orig/calltree/calltree.c calltree-2.3/calltree/calltree.c
 --- calltree-2.3.orig/calltree/calltree.c	2004-01-26 13:39:31.000000000 +0000
 +++ calltree-2.3/calltree/calltree.c	2004-06-08 09:35:34.000000000 +0100
diff -ur w/calltree.build h/calltree.build
--- w/calltree.build	2005-09-21 14:23:45.000000000 +0100
+++ h/calltree.build	2005-09-21 14:07:36.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=2.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
  patch -p1 < ../calltree-2.3.diff
 )
@@ -39,7 +42,7 @@
 #            --sysconfdir=/etc \
 #            --localstatedir=/var/lib
 
-make INS_BASE=/usr
+make
 
 make install INS_BASE=$PKG/usr
 
@@ -103,4 +106,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/celestia.build h/celestia.build
--- w/celestia.build	2004-08-31 14:54:14.000000000 +0100
+++ h/celestia.build	2004-08-31 14:54:14.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=celestia
 VERSION=1.3.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -109,4 +120,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/clamav.build h/clamav.build
--- w/clamav.build	2005-07-25 21:16:35.000000000 +0100
+++ h/clamav.build	2005-09-16 21:19:04.000000000 +0100
@@ -4,7 +4,8 @@
 PKGNAME=clamav
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
-VERSION=0.86.2
+VERSION=0.87
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -26,7 +27,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/clanlib-svn.build h/clanlib-svn.build
--- w/clanlib-svn.build	2005-07-26 14:41:29.000000000 +0100
+++ h/clanlib-svn.build	2005-07-26 14:41:29.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=ClanLib
 VERSION=0.7svn
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION$EXTRAVERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$EXTRAVERSION$PKG_EXT
 (cd $PKGNAME-$VERSION
 ./autogen.sh
 )
@@ -107,4 +118,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/clanlib.build h/clanlib.build
--- w/clanlib.build	2005-02-21 14:21:17.000000000 +0000
+++ h/clanlib.build	2005-02-21 14:21:17.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=ClanLib
 VERSION=0.7.8
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=-1
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION$EXTRAVERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$EXTRAVERSION$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -94,4 +105,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/clanlib6.build h/clanlib6.build
--- w/clanlib6.build	2005-02-24 21:55:41.000000000 +0000
+++ h/clanlib6.build	2005-02-24 21:55:41.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=ClanLib
 VERSION=0.6.5
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=-1
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION$EXTRAVERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$EXTRAVERSION$PKG_EXT
 
 cd $PKGNAME-$VERSION
 cat ../$PKGNAME-$VERSION*.patch | patch -p0
@@ -100,4 +111,4 @@
 (cd $PKG/usr/lib; strip --strip-unneeded *)
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/cln.build h/cln.build
--- w/cln.build	2005-08-05 14:11:18.000000000 +0100
+++ h/cln.build	2005-08-05 14:11:18.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.1.9
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/cyrus-sasl.build h/cyrus-sasl.build
--- w/cyrus-sasl.build	2005-03-18 22:36:20.000000000 +0000
+++ h/cyrus-sasl.build	2005-03-18 22:36:20.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=cyrus-sasl
 VERSION=2.1.20
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/c
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +110,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/d4x.build h/d4x.build
--- w/d4x.build	2004-11-10 21:29:32.000000000 +0000
+++ h/d4x.build	2005-09-16 20:08:43.000000000 +0100
@@ -2,20 +2,31 @@
 #
 CWD=`pwd`
 PKGNAME=d4x
-VERSION=2.5.0final
+VERSION=2.5.5
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/d
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -88,4 +99,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ddd.build h/ddd.build
--- w/ddd.build	2005-05-04 08:30:42.000000000 +0100
+++ h/ddd.build	2005-05-04 08:30:42.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=3.3.11
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnu/ddd
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/directfb.build h/directfb.build
--- w/directfb.build	2005-02-24 21:38:18.000000000 +0000
+++ h/directfb.build	2005-02-24 21:38:18.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=DirectFB
 VERSION=0.9.22
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/d/directfb
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -94,4 +105,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/dirmngr.build h/dirmngr.build
--- w/dirmngr.build	2005-06-24 21:29:43.000000000 +0100
+++ h/dirmngr.build	2005-06-24 21:29:43.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=0.9.2
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/d
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/dosemu.build h/dosemu.build
--- w/dosemu.build	2004-07-29 11:28:18.000000000 +0100
+++ h/dosemu.build	2004-07-29 11:28:18.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=dosemu
 VERSION=1.2.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/d
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tgz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION.tgz
 fi
 
 cd $PKGNAME-$VERSION
@@ -70,4 +81,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/dvd-slideshow.build h/dvd-slideshow.build
--- w/dvd-slideshow.build	2005-03-31 20:46:29.000000000 +0100
+++ h/dvd-slideshow.build	2005-03-31 20:46:29.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=dvd-slideshow
 VERSION=0.7.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/d
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d ${PKGNAME}_$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/${PKGNAME}_$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/${PKGNAME}_$VERSION$PKG_EXT
 
 fi
 
@@ -89,4 +100,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/dvdauthor.build h/dvdauthor.build
--- w/dvdauthor.build	2005-03-01 09:55:31.000000000 +0000
+++ h/dvdauthor.build	2005-03-01 09:55:31.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=dvdauthor
 VERSION=0.6.11
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/d
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -92,4 +103,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/dvdrtools.build h/dvdrtools.build
--- w/dvdrtools.build	2005-03-01 10:09:17.000000000 +0000
+++ h/dvdrtools.build	2005-03-01 10:09:17.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=dvdrtools
 VERSION=0.2.1
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/d
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -92,4 +103,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/dvdstyler.build h/dvdstyler.build
--- w/dvdstyler.build	2005-08-03 19:24:32.000000000 +0100
+++ h/dvdstyler.build	2005-08-03 19:24:32.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=DVDStyler
 VERSION=1.4
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/d
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -96,4 +107,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/dvgrab.build h/dvgrab.build
--- w/dvgrab.build	2005-01-15 19:21:21.000000000 +0000
+++ h/dvgrab.build	2005-01-15 19:21:21.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=dvgrab
 VERSION=1.7
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/d
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/easytag.build h/easytag.build
--- w/easytag.build	2005-07-30 23:07:15.000000000 +0100
+++ h/easytag.build	2005-07-30 23:07:15.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/e
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/enblend.build h/enblend.build
--- w/enblend.build	2005-09-09 15:20:43.000000000 +0100
+++ h/enblend.build	2005-09-09 15:20:43.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/e
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/epstool.build h/epstool.build
--- w/epstool.build	2005-01-15 19:22:49.000000000 +0000
+++ h/epstool.build	2005-01-15 19:22:49.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=epstool
 VERSION=3.07
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/e
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -85,4 +96,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/espgs.build h/espgs.build
--- w/espgs.build	2004-01-18 21:46:31.000000000 +0000
+++ h/espgs.build	2004-01-18 21:46:31.000000000 +0000
@@ -20,7 +20,7 @@
 # is still very much in flux.
 cd $TMP
 rm -rf espgs-$GS_VERSION
-tar xjvf $CWD/espgs-$GS_VERSION-source.tar.bz2
+tar $TAR_OPTS $CWD/espgs-$GS_VERSION-source$PKG_EXT
 cd espgs-$GS_VERSION
 # Make sure -lz is used with -lpng, or PNG detection will fail
 # and PNG devices will not be included.
@@ -46,7 +46,7 @@
 # Now the main source is compiled:
 cd $TMP
 # This should have been untarred when we compiled libijs.
-#tar xjvf $CWD/espgs-$GS_VERSION-source.tar.bz2
+#tar $TAR_OPTS $CWD/espgs-$GS_VERSION-source.tar.bz2
 cd espgs-$GS_VERSION
 
 # Build the version with X11/CUPS/Omni/gimp-print/etc support:
diff -ur w/ethereal.build h/ethereal.build
--- w/ethereal.build	2005-08-12 11:01:53.000000000 +0100
+++ h/ethereal.build	2005-03-25 20:38:08.000000000 +0000
@@ -2,18 +2,21 @@
 #
 CWD=`pwd`
 PKGNAME=ethereal
+VERSION=0.10.10
+VERSIONEXTRA=
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
-VERSION=0.10.12
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/e
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -63,7 +66,7 @@
 
 mkdir -p $PKG/usr/doc/$PKGNAME-$VERSION
 cp -a \
-  AUTHORS* COPYING FAQ INSTALL* NEWS README* \
+  AUTHORS* COPYING* FAQ INSTALL NEWS README* TODO \
   $PKG/usr/doc/$PKGNAME-$VERSION
 
 mkdir -p $PKG/install
@@ -78,7 +81,7 @@
     |-----handy-ruler------------------------------------------------------|
 ethereal: ethereal
 ethereal:
-ethereal:
+ethereal: 
 ethereal:
 ethereal:
 ethereal:
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R `whoami`.users $PKG"
diff -ur w/faac.build h/faac.build
--- w/faac.build	2004-07-23 14:55:18.000000000 +0100
+++ h/faac.build	2004-07-23 14:55:18.000000000 +0100
@@ -3,8 +3,13 @@
 CWD=`pwd`
 PKGNAME=faac
 VERSION=1.24
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
@@ -15,7 +20,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME $PKGNAME-$VERSION
 fi
 
@@ -69,4 +74,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/faad2.build h/faad2.build
--- w/faad2.build	2004-07-23 14:55:26.000000000 +0100
+++ h/faad2.build	2004-07-23 14:55:26.000000000 +0100
@@ -3,8 +3,13 @@
 CWD=`pwd`
 PKGNAME=faad2
 VERSION=2.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
@@ -15,7 +20,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME $PKGNAME-$VERSION
 fi
 
@@ -66,4 +71,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/fam.build h/fam.build
--- w/fam.build	2005-03-19 21:29:39.000000000 +0000
+++ h/fam.build	2005-03-19 21:29:39.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=fam
 VERSION=2.7.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION; chmod -R +w *; rm ltmain.sh; autoreconf -i )
 fi
 
@@ -99,4 +110,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ffmpeg-0.4.8.build h/ffmpeg-0.4.8.build
--- w/ffmpeg-0.4.8.build	2004-07-24 22:24:46.000000000 +0100
+++ h/ffmpeg-0.4.8.build	2004-07-24 22:24:46.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=ffmpeg
 VERSION=0.4.8
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -72,4 +83,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ffmpeg.build h/ffmpeg.build
--- w/ffmpeg.build	2005-09-02 14:21:08.000000000 +0100
+++ h/ffmpeg.build	2004-07-23 22:38:40.000000000 +0100
@@ -2,20 +2,31 @@
 #
 CWD=`pwd`
 PKGNAME=ffmpeg
-VERSION=cvs
+VERSION=0.4.9-pre1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -28,21 +39,23 @@
             --sysconfdir=/etc \
             --localstatedir=/var/lib \
 	    --enable-mp3lame \
-	    --enable-libogg \
 	    --enable-vorbis \
 	    --enable-faad \
 	    --enable-a52 \
 	    --enable-gpl \
 	    --enable-faac \
 	    --enable-pp \
-	    --enable-xvid \
 	    --enable-shared
 make
 cd doc
 make
 cd ..
 
-make install prefix=$PKG/usr bindir=$PKG/usr/bin mandir=$PKG/usr/man libdir=$PKG/usr/lib
+make install prefix=$PKG/usr bindir=$PKG/usr/bin mandir=$PKG/usr/man
+
+# compress man pages
+find $PKG/usr/man -exec gzip {} \;
+
 mkdir -p $PKG/usr/doc/$PKGNAME-$VERSION
 cp -a \
   COPYING CREDITS INSTALL README* doc/TODO doc/*html doc/*conf \
@@ -71,4 +84,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/fftw.build h/fftw.build
--- w/fftw.build	2004-09-16 09:57:52.000000000 +0100
+++ h/fftw.build	2004-09-16 09:57:52.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=fftw
 VERSION=3.0.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -108,4 +119,4 @@
 install-info --info-dir=/usr/info /usr/info/fftw3.info.gz
 EOF
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/flexlay.build h/flexlay.build
--- w/flexlay.build	2005-07-26 13:02:24.000000000 +0100
+++ h/flexlay.build	2005-07-26 13:02:24.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=0.1.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 #(cd $PKGNAME-$VERSION
 #./autogen.sh
 
@@ -109,4 +112,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/flightgear.build h/flightgear.build
--- w/flightgear.build	2004-08-04 21:33:50.000000000 +0100
+++ h/flightgear.build	2005-08-12 21:52:25.000000000 +0100
@@ -2,13 +2,31 @@
 #
 CWD=`pwd`
 PKGNAME=FlightGear
-VERSION=0.9.5
+VERSION=0.9.8
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
+PKG_EXT=.tar.gz
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f/Flightgear
 PKG=/tmp/package-$PKGNAME
-DATA=fgfs-base-0.9.5.tar.gz
+DATA=fgfs-base-0.9.8.tar.bz2
+
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
+if [ ! -f $PKGDIR/$DATA ]
+then
+  echo Can\'t find $PKGDIR/$DATA
+  exit
+fi
 
 rm -rf $PKG
 mkdir -p $PKG
@@ -16,7 +34,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -35,7 +53,7 @@
 
 mkdir -p $PKG/usr/share/FlightGear
 
-(cd $PKG/usr/share/FlightGear; tar xzf $PKGDIR/$DATA)
+(cd $PKG/usr/share/FlightGear; tar xf $PKGDIR/$DATA)
 
 # strip execs
 strip $PKG/usr/bin/*
@@ -47,6 +65,7 @@
 cp -a \
   AUTHORS COPYING INSTALL NEWS README* Thanks docs-mini \
   $PKG/usr/doc/$PKGNAME-$VERSION
+
 mkdir -p $PKG/install
 cat <<EOF > $PKG/install/slack-desc
 # HOW TO EDIT THIS FILE:
@@ -57,18 +76,18 @@
 # customary to leave one space after the ':'.
 
           |-----handy-ruler------------------------------------------------------|
-plib: plib
-plib:
-plib: 
-plib:
-plib:
-plib:
-plib:
-plib:
-plib:
-plib:
-plib:
+FlightGear: FlightGear
+FlightGear:
+FlightGear: 
+FlightGear:
+FlightGear:
+FlightGear:
+FlightGear:
+FlightGear:
+FlightGear:
+FlightGear:
+FlightGear:
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/fltk.build h/fltk.build
--- w/fltk.build	2005-02-15 10:35:14.000000000 +0000
+++ h/fltk.build	2005-02-15 10:35:14.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=fltk
 VERSION=1.1.6
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION-source.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION-source$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -81,4 +92,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/foomatic-db-engine.build h/foomatic-db-engine.build
--- w/foomatic-db-engine.build	2004-12-21 10:14:35.000000000 +0000
+++ h/foomatic-db-engine.build	2004-12-21 10:14:35.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=foomatic-db-engine
 VERSION=3.0.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 SHORTVER=$VERSION
 
 PKGDIR=~/tmp/new/f/foomatic
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/foomatic-db-hpijs.build h/foomatic-db-hpijs.build
--- w/foomatic-db-hpijs.build	2004-12-21 10:15:02.000000000 +0000
+++ h/foomatic-db-hpijs.build	2004-12-21 10:15:02.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=foomatic-db-hpijs
 VERSION=1.4-1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 SHORTVER=$VERSION
 
 PKGDIR=~/tmp/new/f/foomatic
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/foomatic-db.build h/foomatic-db.build
--- w/foomatic-db.build	2004-12-21 10:15:22.000000000 +0000
+++ h/foomatic-db.build	2004-12-21 10:15:22.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=foomatic-db
 VERSION=20041221
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 SHORTVER=$VERSION
 
 PKGDIR=~/tmp/new/f/foomatic
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/foomatic-filters.build h/foomatic-filters.build
--- w/foomatic-filters.build	2004-12-21 10:15:49.000000000 +0000
+++ h/foomatic-filters.build	2004-12-21 10:15:49.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=foomatic-filters
 VERSION=3.0.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 SHORTVER=$VERSION
 
 PKGDIR=~/tmp/new/f/foomatic
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/freepop.build h/freepop.build
--- w/freepop.build	2005-02-21 15:38:28.000000000 +0000
+++ h/freepop.build	2005-02-21 15:38:28.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=freepop
 VERSION=0.6.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION$EXTRAVERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$EXTRAVERSION$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/freetype.build h/freetype.build
--- w/freetype.build	2005-08-18 09:52:48.000000000 +0100
+++ h/freetype.build	2005-08-18 09:52:48.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=freetype
 VERSION=2.1.10
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f/freetype
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cd $PKGNAME-$VERSION
 zcat $CWD/freetype.illadvisederror.diff.gz | patch -p1
 zcat $CWD/freetype.bytecode.diff.gz | patch -p1
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/freetype2.build h/freetype2.build
--- w/freetype2.build	2004-09-22 22:35:59.000000000 +0100
+++ h/freetype2.build	2004-09-22 22:35:59.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=freetype
 VERSION=2.1.9
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f/freetype
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cd $PKGNAME-$VERSION
 zcat $CWD/freetype.illadvisederror.diff.gz | patch -p1
 cat $CWD/freetype.bytecode.diff | patch -p1
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/fribidi.build h/fribidi.build
--- w/fribidi.build	2005-07-30 21:48:15.000000000 +0100
+++ h/fribidi.build	2005-07-30 21:48:15.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.10.5
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/frontline.build h/frontline.build
--- w/frontline.build	2005-07-29 15:10:25.000000000 +0100
+++ h/frontline.build	2005-07-29 15:10:25.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.5.4
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/g-wrap.build h/g-wrap.build
--- w/g-wrap.build	2005-09-23 20:52:11.000000000 +0100
+++ h/g-wrap.build	2005-09-22 15:02:23.000000000 +0100
@@ -4,16 +4,19 @@
 PKGNAME=g-wrap
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
-VERSION=1.3.4
+VERSION=1.9.6
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gambas.build h/gambas.build
--- w/gambas.build	2005-01-15 19:24:06.000000000 +0000
+++ h/gambas.build	2005-01-15 19:24:06.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=gambas
 VERSION=1.0.2
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cp gambas.Makefile.in $PKGNAME-$VERSION/Makefile.in
 fi
 
@@ -93,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gc.build h/gc.build
--- w/gc.build	2005-07-28 13:21:25.000000000 +0100
+++ h/gc.build	2005-07-28 13:21:25.000000000 +0100
@@ -5,8 +5,11 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=6.5
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
@@ -101,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gd.build h/gd.build
--- w/gd.build	2005-05-12 13:56:51.000000000 +0100
+++ h/gd.build	2005-05-12 13:56:51.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.0.33
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gerbv.build h/gerbv.build
--- w/gerbv.build	2005-03-31 11:54:01.000000000 +0100
+++ h/gerbv.build	2005-03-31 11:54:01.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=gerbv
 VERSION=1.0.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -94,4 +105,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gftp.build h/gftp.build
--- w/gftp.build	2005-02-05 20:07:50.000000000 +0000
+++ h/gftp.build	2005-02-05 20:07:50.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=gftp
 VERSION=2.0.18
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ghostscript.build h/ghostscript.build
--- w/ghostscript.build	2005-05-15 21:19:32.000000000 +0100
+++ h/ghostscript.build	2005-05-15 21:19:32.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=ghostscript
 VERSION=8.51
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 SHORTVER=$VERSION
 
 PKGDIR=~/tmp/new/g/
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 #cd $PKGNAME-$VERSION
 #patch -p1 < ../ghostscript-cups.diff
 #autogen.sh --prefix=/usr
@@ -118,4 +129,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gimp-print.build h/gimp-print.build
--- w/gimp-print.build	2004-12-21 20:31:58.000000000 +0000
+++ h/gimp-print.build	2004-12-21 20:31:58.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=gimp-print
 VERSION=4.2.7
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 SHORTVER=$VERSION
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -107,4 +118,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gimp.build h/gimp.build
--- w/gimp.build	2005-07-02 21:16:50.000000000 +0100
+++ h/gimp.build	2005-07-02 21:16:50.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=gimp
 VERSION=2.2.8
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g/gimp
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +109,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/glame.build h/glame.build
--- w/glame.build	2005-03-28 22:19:29.000000000 +0100
+++ h/glame.build	2005-03-28 22:19:29.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=glame
 VERSION=2.0.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -72,4 +83,4 @@
 install-info --info-dir=/usr/info /usr/info/glame-dev.info.gz
 EOF
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/glib2.build h/glib2.build
--- w/glib2.build	2004-12-23 21:27:57.000000000 +0000
+++ h/glib2.build	2004-12-23 21:27:57.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=glib
 VERSION=2.4.8
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnome/2.6.2-post
 PKG=/tmp/package-${PKGNAME}2
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg ${PKGNAME}2-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg ${PKGNAME}2-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/glibmm.build h/glibmm.build
--- w/glibmm.build	2005-07-28 13:43:18.000000000 +0100
+++ h/glibmm.build	2005-07-28 13:43:18.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=2.6.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -101,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gmult.build h/gmult.build
--- w/gmult.build	2005-01-30 20:18:18.000000000 +0000
+++ h/gmult.build	2005-01-30 20:18:18.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=gmult
 VERSION=4.0
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gnucash.build h/gnucash.build
--- w/gnucash.build	2005-09-25 20:21:08.000000000 +0100
+++ h/gnucash.build	2005-09-25 20:21:09.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.8.11
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 #(cd $PKGNAME-$VERSION
 #./autogen.sh
 
@@ -123,4 +126,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gnupg19.build h/gnupg19.build
--- w/gnupg19.build	2005-06-24 21:51:05.000000000 +0100
+++ h/gnupg19.build	2005-06-24 21:51:05.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.9.17
+VERSIONEXTRA=
 ARCH=i486
 BUILD=2
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/gnutls.build h/gnutls.build
--- w/gnutls.build	2005-06-15 20:05:25.000000000 +0100
+++ h/gnutls.build	2005-06-15 20:05:25.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.2.4
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/gperf.build h/gperf.build
--- w/gperf.build	2004-09-06 10:52:26.000000000 +0100
+++ h/gperf.build	2004-09-06 10:52:26.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=gperf
 VERSION=3.0.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnu/gperf
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -70,4 +81,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gpgme.build h/gpgme.build
--- w/gpgme.build	2005-06-24 21:35:27.000000000 +0100
+++ h/gpgme.build	2005-06-24 21:35:27.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.0.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/gphoto2.build h/gphoto2.build
--- w/gphoto2.build	2005-03-18 22:00:02.000000000 +0000
+++ h/gphoto2.build	2005-03-18 22:00:02.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=gphoto2
 VERSION=2.1.5
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/graphviz.build h/graphviz.build
--- w/graphviz.build	2005-08-01 20:18:04.000000000 +0100
+++ h/graphviz.build	2005-08-01 20:18:04.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.4
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/grip.build h/grip.build
--- w/grip.build	2005-02-20 20:52:29.000000000 +0000
+++ h/grip.build	2005-02-20 20:52:29.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=grip
 VERSION=3.2.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gsl.build h/gsl.build
--- w/gsl.build	2005-06-21 09:01:15.000000000 +0100
+++ h/gsl.build	2005-06-21 09:01:15.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.6
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnu/gsl
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gsview.build h/gsview.build
--- w/gsview.build	2005-04-11 20:50:13.000000000 +0100
+++ h/gsview.build	2005-04-11 20:50:13.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=gsview
 VERSION=4.7
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 #cd $PKGNAME-$VERSION
 #patch -p1 < ../gsview-4.6-patch1.txt
 #cd ..
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gtk+2.build h/gtk+2.build
--- w/gtk+2.build	2004-12-23 22:32:37.000000000 +0000
+++ h/gtk+2.build	2004-12-23 22:32:37.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=gtk+
 VERSION=2.4.14
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnome/2.6.2-post
 PKG=/tmp/package-${PKGNAME}2
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -116,4 +127,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg -p ${PKGNAME}2-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg -p ${PKGNAME}2-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gtk-sharp.build h/gtk-sharp.build
--- w/gtk-sharp.build	2005-09-09 13:55:47.000000000 +0100
+++ h/gtk-sharp.build	2005-09-09 13:55:47.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.0.10
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m/mono
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gtk-sharp2.build h/gtk-sharp2.build
--- w/gtk-sharp2.build	2005-09-09 13:55:36.000000000 +0100
+++ h/gtk-sharp2.build	2005-09-09 13:55:36.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.3.90
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m/mono
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gtkmm.build h/gtkmm.build
--- w/gtkmm.build	2005-07-28 14:46:15.000000000 +0100
+++ h/gtkmm.build	2005-07-28 14:46:15.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=2.6.4
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -101,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/gurlchecker.build h/gurlchecker.build
--- w/gurlchecker.build	2005-06-15 19:54:06.000000000 +0100
+++ h/gurlchecker.build	2005-06-15 19:54:06.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.8.2
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/gwenview.build h/gwenview.build
--- w/gwenview.build	2005-05-11 12:56:19.000000000 +0100
+++ h/gwenview.build	2005-05-11 12:56:19.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.2.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/g
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -100,4 +103,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/hermes.build h/hermes.build
--- w/hermes.build	2005-02-23 15:54:51.000000000 +0000
+++ h/hermes.build	2005-02-23 15:54:51.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=Hermes
 VERSION=1.3.3
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/h
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 cd $PKGNAME-$VERSION
 aclocal
@@ -99,4 +110,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/hpijs.build h/hpijs.build
--- w/hpijs.build	2005-05-15 21:41:40.000000000 +0100
+++ h/hpijs.build	2005-05-15 21:41:40.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.7.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/h
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/hplip.build h/hplip.build
--- w/hplip.build	2005-05-28 22:52:27.000000000 +0100
+++ h/hplip.build	2005-05-28 22:52:27.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.9.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/h
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/htmldoc.build h/htmldoc.build
--- w/htmldoc.build	2005-02-15 10:49:38.000000000 +0000
+++ h/htmldoc.build	2005-02-15 10:49:38.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=htmldoc
 VERSION=1.8.24
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/h
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION-source.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION-source$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -74,4 +85,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/hugin.build h/hugin.build
--- w/hugin.build	2005-09-09 13:11:54.000000000 +0100
+++ h/hugin.build	2005-09-09 21:47:43.000000000 +0100
@@ -2,18 +2,21 @@
 #
 CWD=`pwd`
 PKGNAME=hugin
-PKG_EXT=.tar.bz2
-TAR_OPTS=xjvf
-VERSION=0.5_cvs050602
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
+VERSION=0.5_cvs050908
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/h
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,8 +26,14 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv hugin-0.5 $PKGNAME-$VERSION
+(cd $PKGNAME-$VERSION
+ if [ ! -f configure ]
+ then
+   bootstrap
+ fi
+)
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +108,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/i2c.build h/i2c.build
--- w/i2c.build	2005-06-21 10:45:07.000000000 +0100
+++ h/i2c.build	2005-06-21 10:45:07.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.9.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/i
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/id3lib.build h/id3lib.build
--- w/id3lib.build	2004-07-26 19:54:31.000000000 +0100
+++ h/id3lib.build	2004-07-26 19:54:31.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=id3lib
 VERSION=3.8.3
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/i
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -58,4 +69,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/imlib2.build h/imlib2.build
--- w/imlib2.build	2004-07-24 22:07:55.000000000 +0100
+++ h/imlib2.build	2004-07-24 22:07:55.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=imlib2
 VERSION=1.1.1-pre1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/i
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME-${VERSION/%-*/} $PKGNAME-$VERSION
 fi
 
@@ -59,4 +70,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ink.build h/ink.build
--- w/ink.build	2005-05-12 22:41:26.000000000 +0100
+++ h/ink.build	2005-05-12 22:41:26.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=ink
 VERSION=0.3
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/i
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME $PKGNAME-$VERSION
 fi
 
diff -ur w/inkscape.build h/inkscape.build
--- w/inkscape.build	2005-07-28 15:08:17.000000000 +0100
+++ h/inkscape.build	2005-07-28 15:08:17.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=0.42
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/i
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/iperf.build h/iperf.build
--- w/iperf.build	2005-06-14 13:15:22.000000000 +0100
+++ h/iperf.build	2005-06-14 13:15:22.000000000 +0100
@@ -5,17 +5,20 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.0.2
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 USER=`id -un`
 GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/i
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +28,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/jack-ack.build h/jack-ack.build
--- w/jack-ack.build	2005-06-03 12:44:20.000000000 +0100
+++ h/jack-ack.build	2005-06-03 12:44:20.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.99.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/j
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/jam.build h/jam.build
--- w/jam.build	2005-02-18 14:19:51.000000000 +0000
+++ h/jam.build	2005-02-18 14:19:51.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=jam
 VERSION=2.5
+VERSIONEXTRA=
+PKG_EXT=.tar
+TAR_OPTS=xvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/j
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xvf $PKGDIR/$PKGNAME-$VERSION.tar
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -70,4 +81,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/jpeg-mmx.build h/jpeg-mmx.build
--- w/jpeg-mmx.build	2005-01-07 14:08:11.000000000 +0000
+++ h/jpeg-mmx.build	2005-01-07 14:08:11.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=jpeg-mmx
 VERSION=0.1.5
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/j
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 #rm -rf $PKG
 #mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -96,4 +107,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/k3b.build h/k3b.build
--- w/k3b.build	2005-09-12 16:04:10.000000000 +0100
+++ h/k3b.build	2005-08-03 21:51:27.000000000 +0100
@@ -2,20 +2,32 @@
 #
 CWD=`pwd`
 PKGNAME=k3b
-VERSION=0.12.4a
+VERSION=0.12.3
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+#(cd $PKGNAME-$VERSION; patch -p1 < ../k3b-0.11.22.diff )
 fi
 
 cd $PKGNAME-$VERSION
@@ -26,7 +38,7 @@
 CXXFLAGS="-O2 -march=i486 -mcpu=i686" \
 ./configure --prefix=/opt/kde
 
-make -j4
+make -j3
 
 make install DESTDIR=$PKG
 
@@ -80,4 +92,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/kalarm.build h/kalarm.build
--- w/kalarm.build	2005-06-08 11:40:55.000000000 +0100
+++ h/kalarm.build	2005-06-08 11:40:55.000000000 +0100
@@ -8,6 +8,8 @@
 VERSIONEXTRA=.kde3
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
@@ -97,4 +99,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/kavi2svcd.build h/kavi2svcd.build
--- w/kavi2svcd.build	2005-01-06 15:36:36.000000000 +0000
+++ h/kavi2svcd.build	2005-01-06 15:36:36.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=kavi2svcd
 VERSION=0.8.3
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tgz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION.tgz
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/kbarcode.build h/kbarcode.build
--- w/kbarcode.build	2005-08-05 08:40:03.000000000 +0100
+++ h/kbarcode.build	2005-08-04 20:19:20.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.9.7
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/kexi.build h/kexi.build
--- w/kexi.build	2005-06-01 12:26:36.000000000 +0100
+++ h/kexi.build	2005-06-01 12:26:36.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=0.9
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/keximdb.build h/keximdb.build
--- w/keximdb.build	2005-06-01 13:30:13.000000000 +0100
+++ h/keximdb.build	2005-06-01 13:30:13.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.9
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/kink.build h/kink.build
--- w/kink.build	2005-05-14 22:46:00.000000000 +0100
+++ h/kink.build	2005-05-14 22:46:00.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=0.2.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/kino-dvtitler.build h/kino-dvtitler.build
--- w/kino-dvtitler.build	2005-06-25 21:00:32.000000000 +0100
+++ h/kino-dvtitler.build	2005-06-25 21:00:32.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.2.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/k/kino
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/kino-smilutils.build h/kino-smilutils.build
--- w/kino-smilutils.build	2004-07-25 21:15:57.000000000 +0100
+++ h/kino-smilutils.build	2004-07-25 21:15:57.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=smilutils
 VERSION=0.3.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k/kino
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +109,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/kino.build h/kino.build
--- w/kino.build	2005-06-25 20:45:10.000000000 +0100
+++ h/kino.build	2005-06-25 20:45:10.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.7.6
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/kinoplus.build h/kinoplus.build
--- w/kinoplus.build	2005-06-25 23:01:54.000000000 +0100
+++ h/kinoplus.build	2005-06-25 23:01:54.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.3.5
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/k/kino
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/kwave.build h/kwave.build
--- w/kwave.build	2005-06-21 08:57:15.000000000 +0100
+++ h/kwave.build	2005-06-21 08:57:15.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.7.2
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 #mv $PKGNAME-${VERSION/-*/} $PKGNAME-$VERSION
 fi
 
@@ -72,4 +75,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/kxmleditor.build h/kxmleditor.build
--- w/kxmleditor.build	2005-01-31 20:52:46.000000000 +0000
+++ h/kxmleditor.build	2005-01-31 20:52:46.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=kxmleditor
 VERSION=1.1.4
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/k
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -80,4 +91,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ladspa_sdk.build h/ladspa_sdk.build
--- w/ladspa_sdk.build	2004-07-23 13:51:08.000000000 +0100
+++ h/ladspa_sdk.build	2004-07-23 13:51:08.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=ladspa_sdk
 VERSION=1.12
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME_$VERSION.tgz
+tar $TAR_OPTS $PKGDIR/$PKGNAME_$VERSION.tgz
 mv $PKGNAME $PKGNAME-$VERSION
 fi
 
@@ -66,4 +77,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/lame.build h/lame.build
--- w/lame.build	2004-09-07 21:45:54.000000000 +0100
+++ h/lame.build	2004-09-07 21:45:54.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=lame
 VERSION=3.96.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -67,4 +78,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/lcms.build h/lcms.build
--- w/lcms.build	2004-12-31 20:13:44.000000000 +0000
+++ h/lcms.build	2004-12-31 20:13:44.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=lcms
 VERSION=1.14
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libassuan.build h/libassuan.build
--- w/libassuan.build	2005-06-24 21:06:06.000000000 +0100
+++ h/libassuan.build	2005-06-24 21:06:06.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libassuan
 VERSION=0.6.10
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libavc1394.build h/libavc1394.build
--- w/libavc1394.build	2005-06-25 20:47:03.000000000 +0100
+++ h/libavc1394.build	2005-06-25 20:47:03.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.5.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/libcddb.build h/libcddb.build
--- w/libcddb.build	2005-01-06 15:31:05.000000000 +0000
+++ h/libcddb.build	2005-01-06 15:31:05.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libcddb
 VERSION=0.9.6
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libcdio-basic.build h/libcdio-basic.build
--- w/libcdio-basic.build	2005-01-07 21:01:16.000000000 +0000
+++ h/libcdio-basic.build	2005-01-07 21:01:16.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libcdio
 VERSION=0.71
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnu/libcdio
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -92,4 +103,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libcdio.build h/libcdio.build
--- w/libcdio.build	2005-01-07 21:01:23.000000000 +0000
+++ h/libcdio.build	2005-01-07 21:01:23.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libcdio
 VERSION=0.71
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnu/libcdio
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libdc1394.build h/libdc1394.build
--- w/libdc1394.build	2005-06-25 20:55:05.000000000 +0100
+++ h/libdc1394.build	2005-06-25 20:55:05.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.1.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/libdv.build h/libdv.build
--- w/libdv.build	2005-01-07 20:58:07.000000000 +0000
+++ h/libdv.build	2005-01-07 20:58:07.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libdv
 VERSION=0.104
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -60,4 +71,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libemf.build h/libemf.build
--- w/libemf.build	2004-12-18 21:42:22.000000000 +0000
+++ h/libemf.build	2004-12-18 21:42:22.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libEMF
 VERSION=1.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cd $PKGNAME-$VERSION
 patch -p1 < ../libEMF.diff
 cd ..
@@ -93,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libexif.build h/libexif.build
--- w/libexif.build	2005-05-29 08:53:45.000000000 +0100
+++ h/libexif.build	2005-05-29 08:53:45.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libexif
 VERSION=0.6.12
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -82,4 +93,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libfame.build h/libfame.build
--- w/libfame.build	2005-01-07 10:20:07.000000000 +0000
+++ h/libfame.build	2005-01-07 10:20:07.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libfame
 VERSION=0.9.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libgcrypt.build h/libgcrypt.build
--- w/libgcrypt.build	2005-03-20 21:29:30.000000000 +0000
+++ h/libgcrypt.build	2005-03-20 21:29:30.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libgcrypt
 VERSION=1.2.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libgdiplus.build h/libgdiplus.build
--- w/libgdiplus.build	2005-09-09 12:39:01.000000000 +0100
+++ h/libgdiplus.build	2005-09-09 12:39:01.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.1.8
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m/mono
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libgpg-error.build h/libgpg-error.build
--- w/libgpg-error.build	2005-03-20 21:25:14.000000000 +0000
+++ h/libgpg-error.build	2005-03-20 21:25:14.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libgpg-error
 VERSION=1.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libgphoto2.build h/libgphoto2.build
--- w/libgphoto2.build	2005-03-18 21:49:44.000000000 +0000
+++ h/libgphoto2.build	2005-03-18 21:49:44.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libgphoto2
 VERSION=2.1.5
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l/
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -93,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libidn.build h/libidn.build
--- w/libidn.build	2005-05-26 21:14:24.000000000 +0100
+++ h/libidn.build	2005-05-26 21:14:24.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libidn
 VERSION=0.5.17
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -73,4 +84,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libinklevel.build h/libinklevel.build
--- w/libinklevel.build	2005-05-12 22:33:54.000000000 +0100
+++ h/libinklevel.build	2005-05-12 22:33:54.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libinklevel
 VERSION=0.6.4
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME $PKGNAME-$VERSION
 fi
 
diff -ur w/libksba.build h/libksba.build
--- w/libksba.build	2005-06-24 21:07:52.000000000 +0100
+++ h/libksba.build	2005-06-24 21:07:52.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libksba
 VERSION=0.9.11
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libmikmod.build h/libmikmod.build
--- w/libmikmod.build	2005-02-21 18:23:36.000000000 +0000
+++ h/libmikmod.build	2005-02-21 18:23:36.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libmikmod
 VERSION=3.1.11
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libpano12.build h/libpano12.build
--- w/libpano12.build	2005-09-09 13:08:08.000000000 +0100
+++ h/libpano12.build	2005-09-09 19:05:10.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=2.7.0.10
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libpng.build h/libpng.build
--- w/libpng.build	2005-08-01 20:09:02.000000000 +0100
+++ h/libpng.build	2005-08-01 20:09:02.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.2.8
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1a
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
  patch -p0 < ../libpng.diff
  ln -s scripts/makefile.linux Makefile
diff -ur w/libqalculate.build h/libqalculate.build
--- w/libqalculate.build	2005-08-05 13:34:24.000000000 +0100
+++ h/libqalculate.build	2005-08-05 13:34:24.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.8.1.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/libquicktime.build h/libquicktime.build
--- w/libquicktime.build	2005-01-15 20:45:22.000000000 +0000
+++ h/libquicktime.build	2005-01-15 20:45:22.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libquicktime
 VERSION=0.9.4
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libraw1394.build h/libraw1394.build
--- w/libraw1394.build	2005-06-25 20:49:25.000000000 +0100
+++ h/libraw1394.build	2005-06-25 20:49:25.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.2.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/libsamplerate.build h/libsamplerate.build
--- w/libsamplerate.build	2004-11-07 08:13:42.000000000 +0000
+++ h/libsamplerate.build	2004-11-07 08:13:42.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libsamplerate
 VERSION=0.1.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -66,4 +77,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libsigc++.build h/libsigc++.build
--- w/libsigc++.build	2005-07-28 13:35:30.000000000 +0100
+++ h/libsigc++.build	2005-07-28 13:35:30.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=2.0.15
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -101,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libsndfile.build h/libsndfile.build
--- w/libsndfile.build	2004-11-19 22:24:01.000000000 +0000
+++ h/libsndfile.build	2004-11-19 22:24:01.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libsndfile
 VERSION=1.0.11
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -65,4 +76,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libtheora.build h/libtheora.build
--- w/libtheora.build	2005-01-07 11:12:39.000000000 +0000
+++ h/libtheora.build	2005-01-07 11:12:39.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libtheora
 VERSION=1.0alpha4
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION; patch -p1 < ../libtheora.diff)
 fi
 
@@ -94,4 +105,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libtiff.build h/libtiff.build
--- w/libtiff.build	2005-03-20 20:09:57.000000000 +0000
+++ h/libtiff.build	2005-03-20 20:09:57.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=tiff
 VERSION=3.7.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/t
 PKG=/tmp/package-lib$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +109,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg lib$PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg lib$PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libwmf.build h/libwmf.build
--- w/libwmf.build	2004-12-23 20:58:40.000000000 +0000
+++ h/libwmf.build	2004-12-23 20:58:40.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libwmf
 VERSION=0.2.8.3
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -94,4 +105,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libxml++-1.build h/libxml++-1.build
--- w/libxml++-1.build	2005-02-27 21:36:45.000000000 +0000
+++ h/libxml++-1.build	2005-02-27 21:36:45.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libxml++
 VERSION=1.0.4
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnome/
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libxml++.build h/libxml++.build
--- w/libxml++.build	2005-02-27 21:33:53.000000000 +0000
+++ h/libxml++.build	2005-02-27 21:33:53.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=libxml++
 VERSION=2.6.1
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnome/
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libxml2.build h/libxml2.build
--- w/libxml2.build	2005-07-26 13:54:57.000000000 +0100
+++ h/libxml2.build	2005-07-26 13:54:57.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=2.6.20
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/libxslt.build h/libxslt.build
--- w/libxslt.build	2005-07-26 14:02:50.000000000 +0100
+++ h/libxslt.build	2005-07-26 14:02:50.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.1.14
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/lincity-ng.build h/lincity-ng.build
--- w/lincity-ng.build	2005-06-24 22:12:51.000000000 +0100
+++ h/lincity-ng.build	2005-06-24 22:12:51.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/lincvs.build h/lincvs.build
--- w/lincvs.build	2005-03-10 11:18:19.000000000 +0000
+++ h/lincvs.build	2005-03-10 11:18:19.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=lincvs
 VERSION=1.4.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=0-generic-src
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION-$EXTRAVERSION.tgz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION-$EXTRAVERSION.tgz
 
 fi
 
@@ -104,4 +115,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/linkchecker.build h/linkchecker.build
--- w/linkchecker.build	2005-06-15 18:44:26.000000000 +0100
+++ h/linkchecker.build	2005-06-15 18:44:26.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.9
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/lm_sensors.build h/lm_sensors.build
--- w/lm_sensors.build	2005-06-21 10:58:31.000000000 +0100
+++ h/lm_sensors.build	2005-06-21 10:58:31.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=2.9.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -101,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/lphoto.build h/lphoto.build
--- w/lphoto.build	2005-03-14 22:44:41.000000000 +0000
+++ h/lphoto.build	2005-03-14 22:44:41.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=lphoto
 VERSION=2.0.42
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=0.0.0.45.lindows0.1
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/${PKGNAME}_$VERSION-$EXTRAVERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/${PKGNAME}_$VERSION-$EXTRAVERSION$PKG_EXT
 
 fi
 
@@ -92,4 +103,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/maxima.build h/maxima.build
--- w/maxima.build	2005-01-06 10:10:49.000000000 +0000
+++ h/maxima.build	2005-01-06 10:10:49.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=maxima
 VERSION=5.9.1
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/mjpegtools.build h/mjpegtools.build
--- w/mjpegtools.build	2005-01-07 20:56:06.000000000 +0000
+++ h/mjpegtools.build	2005-01-07 20:56:06.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=mjpegtools
 VERSION=1.6.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m/mjpegtools
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 #fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/mono.build h/mono.build
--- w/mono.build	2005-09-09 12:06:44.000000000 +0100
+++ h/mono.build	2005-09-09 12:06:44.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.1.8.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m/mono
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/mozplugger.build h/mozplugger.build
--- w/mozplugger.build	2005-04-17 21:06:16.000000000 +0100
+++ h/mozplugger.build	2005-04-17 21:06:16.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=mozplugger
 VERSION=1.7.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -68,4 +79,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/mpgtx.build h/mpgtx.build
--- w/mpgtx.build	2005-03-31 11:32:34.000000000 +0100
+++ h/mpgtx.build	2005-03-31 11:32:34.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=mpgtx
 VERSION=1.3.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/mplayer.build h/mplayer.build
--- w/mplayer.build	2005-09-02 11:55:25.000000000 +0100
+++ h/mplayer.build	2005-09-02 11:55:25.000000000 +0100
@@ -3,12 +3,23 @@
 CWD=`pwd`
 PKGNAME=MPlayer
 VERSION=1.0pre7try2
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/m/MPlayer
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 CODECS=all-20050412
 SKIN=Blue-1.4
 FONT=font-arial-iso-8859-1
@@ -19,7 +30,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
  patch -p1 < ../mplayer.diff
 )
@@ -61,7 +72,7 @@
 
 
 mkdir -p $PKG/usr/lib/codecs
-tar xjf $PKGDIR/codecs/$CODECS.tar.bz2
+tar xjf $PKGDIR/codecs/$CODECS$PKG_EXT
 mv $CODECS/* $PKG/usr/lib/codecs
 
 mkdir -p $PKG/usr/share/mplayer/font
@@ -94,4 +105,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ncurses.build h/ncurses.build
--- w/ncurses.build	2005-06-13 18:39:46.000000000 +0100
+++ h/ncurses.build	2005-06-13 18:39:46.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=5.4
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnu/ncurses/
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/net-snmp.build h/net-snmp.build
--- w/net-snmp.build	2005-05-15 22:30:56.000000000 +0100
+++ h/net-snmp.build	2005-05-15 22:30:56.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=5.1.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/n
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/netpanzer.build h/netpanzer.build
--- w/netpanzer.build	2005-02-18 14:58:09.000000000 +0000
+++ h/netpanzer.build	2005-02-18 14:58:09.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=netpanzer
 VERSION=0.8
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/n
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -78,4 +89,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/netpanzerdata.build h/netpanzerdata.build
--- w/netpanzerdata.build	2005-02-18 15:03:55.000000000 +0000
+++ h/netpanzerdata.build	2005-02-18 15:03:55.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=netpanzer-data
 VERSION=0.8
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/n
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -74,4 +85,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ntfsprogs.build h/ntfsprogs.build
--- w/ntfsprogs.build	2004-09-24 22:44:38.000000000 +0100
+++ h/ntfsprogs.build	2004-09-24 22:44:38.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=ntfsprogs
 VERSION=1.9.4
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/n
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -82,4 +93,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/nvu.build h/nvu.build
--- w/nvu.build	2005-07-19 11:59:12.000000000 +0100
+++ h/nvu.build	2005-07-19 11:59:12.000000000 +0100
@@ -5,9 +5,12 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=1.0
+VERSIONEXTRA=
 VERSION_EXTRA=-sources
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/n
 PKG=/tmp/package-$PKGNAME
@@ -123,4 +126,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/openal-cvs.build h/openal-cvs.build
--- w/openal-cvs.build	2005-08-11 13:26:08.000000000 +0100
+++ h/openal-cvs.build	2005-08-11 13:26:08.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=cvs
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/o
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION/linux
 ./autogen.sh
 )
@@ -108,4 +111,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/openal.build h/openal.build
--- w/openal.build	2004-08-02 22:04:15.000000000 +0100
+++ h/openal.build	2004-08-02 22:04:15.000000000 +0100
@@ -3,8 +3,13 @@
 CWD=`pwd`
 PKGNAME=openal
 VERSION=20040802	
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=2
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/o
 PKG=/tmp/package-$PKGNAME
@@ -15,7 +20,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME $PKGNAME-$VERSION
 fi
 
@@ -99,4 +104,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/openldap.build h/openldap.build
--- w/openldap.build	2005-05-29 09:34:30.000000000 +0100
+++ h/openldap.build	2005-05-29 09:34:30.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=openldap
 VERSION=2.2.26
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/o
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tgz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION.tgz
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/openslp.build h/openslp.build
--- w/openslp.build	2005-05-29 08:52:03.000000000 +0100
+++ h/openslp.build	2005-05-29 08:52:03.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=openslp
 VERSION=1.2.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/o
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -113,4 +124,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/openswan.build h/openswan.build
--- w/openswan.build	2005-02-14 15:39:10.000000000 +0000
+++ h/openswan.build	2005-02-14 15:39:10.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=openswan
 VERSION=2.3.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/o/OpenSwan
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -87,4 +98,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/openvrml.build h/openvrml.build
--- w/openvrml.build	2005-04-21 15:47:58.000000000 +0100
+++ h/openvrml.build	2005-04-21 15:47:58.000000000 +0100
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=openvrml
 VERSION=0.15.7
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/o
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +109,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/pango.build h/pango.build
--- w/pango.build	2004-12-23 22:33:39.000000000 +0000
+++ h/pango.build	2004-12-23 22:33:39.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=pango
 VERSION=1.4.1
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnome/2.6.2-post
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +109,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg -p $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg -p $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/paragui.build h/paragui.build
--- w/paragui.build	2004-11-18 19:24:28.000000000 +0000
+++ h/paragui.build	2004-11-18 19:24:28.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=paragui
 VERSION=1.0.4
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -83,4 +94,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/pdf417.build h/pdf417.build
--- w/pdf417.build	2005-08-05 08:54:47.000000000 +0100
+++ h/pdf417.build	2005-08-05 08:54:47.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=4.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
diff -ur w/physfs.build h/physfs.build
--- w/physfs.build	2005-02-18 21:20:25.000000000 +0000
+++ h/physfs.build	2005-02-18 21:20:25.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=physfs
 VERSION=1.0.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -110,4 +121,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root $PKG; makepkg -l y -c y  $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/pinentry.build h/pinentry.build
--- w/pinentry.build	2005-03-23 21:49:48.000000000 +0000
+++ h/pinentry.build	2005-03-23 21:49:48.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=pinentry
 VERSION=0.7.2
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/pingus.build h/pingus.build
--- w/pingus.build	2005-02-24 15:18:47.000000000 +0000
+++ h/pingus.build	2005-02-24 15:18:47.000000000 +0000
@@ -3,20 +3,31 @@
 CWD=`pwd`
 PKGNAME=pingus
 VERSION=0.6.0
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 cd $PKGNAME-$VERSION
 cat ../$PKGNAME-$VERSION-*.dif | patch -p0
@@ -96,4 +107,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/plib-examples.build h/plib-examples.build
--- w/plib-examples.build	2004-08-05 20:49:20.000000000 +0100
+++ h/plib-examples.build	2004-08-05 20:49:20.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=plib_examples
 VERSION=1.8.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -66,4 +77,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/plib.build h/plib.build
--- w/plib.build	2004-08-02 13:41:28.000000000 +0100
+++ h/plib.build	2005-08-12 21:19:51.000000000 +0100
@@ -2,20 +2,31 @@
 #
 CWD=`pwd`
 PKGNAME=plib
-VERSION=1.8.3
+VERSION=1.8.4
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -66,4 +77,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/plotutils.build h/plotutils.build
--- w/plotutils.build	2004-12-19 07:34:48.000000000 +0000
+++ h/plotutils.build	2004-12-19 07:34:48.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=plotutils
 VERSION=2.4.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=2
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/gnu/plotutils
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cd $PKGNAME-$VERSION
 patch -p1 < ../plotutils-c++.patch
 cd ..
@@ -99,4 +110,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ppracer.build h/ppracer.build
--- w/ppracer.build	2004-12-28 21:29:19.000000000 +0000
+++ h/ppracer.build	2004-12-28 21:29:19.000000000 +0000
@@ -4,13 +4,23 @@
 PKGNAME=ppracer
 VERSION=0.2.2
 VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 VERSIONPKG=$VERSION$VERSIONEXTRA
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 DATAFILE=$PKGDIR/$PKGNAME-data-$VERSION.tar.bz2
 
 rm -rf $PKG
@@ -19,7 +29,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSIONPKG.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSIONPKG$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -92,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ppracer3.build h/ppracer3.build
--- w/ppracer3.build	2005-03-01 20:17:36.000000000 +0000
+++ h/ppracer3.build	2005-03-01 20:17:36.000000000 +0000
@@ -4,20 +4,30 @@
 PKGNAME=ppracer
 VERSION=0.3.1
 VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 VERSIONPKG=$VERSION$VERSIONEXTRA
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSIONPKG.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSIONPKG$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -86,4 +96,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/pstoedit.build h/pstoedit.build
--- w/pstoedit.build	2004-12-21 23:11:46.000000000 +0000
+++ h/pstoedit.build	2004-12-21 23:11:46.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=pstoedit
 VERSION=3.33
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -99,4 +110,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/pygame.build h/pygame.build
--- w/pygame.build	2005-09-16 11:14:36.000000000 +0100
+++ h/pygame.build	2005-09-16 11:14:36.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.7.1release
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/p
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -92,4 +95,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/pygtk.build h/pygtk.build
--- w/pygtk.build	2005-08-05 15:12:07.000000000 +0100
+++ h/pygtk.build	2005-09-03 19:55:19.000000000 +0100
@@ -4,18 +4,19 @@
 PKGNAME=pygtk
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
-VERSION=2.4.1
+VERSION=2.6.2
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
 GROUP=`id -gn`
 
-PKGDIR=~/tmp/new/p
+PKGDIR=~/tmp/gnome/2.10.2/bindings/python
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/qalculate-kde.build h/qalculate-kde.build
--- w/qalculate-kde.build	2005-08-05 14:38:34.000000000 +0100
+++ h/qalculate-kde.build	2005-08-05 14:38:34.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.8.1.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/q
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/qnetwalk.build h/qnetwalk.build
--- w/qnetwalk.build	2005-03-15 22:17:49.000000000 +0000
+++ h/qnetwalk.build	2005-03-15 22:17:49.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=qnetwalk
 VERSION=1.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=2
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/q
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -64,4 +75,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/qtiplot.build h/qtiplot.build
--- w/qtiplot.build	2005-08-17 15:26:47.000000000 +0100
+++ h/qtiplot.build	2005-08-17 15:26:47.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.zip
 TAR_OPTS=xzvf
 VERSION=0.6.7
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/q
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-unzip $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+unzip $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
  patch -p0 < ../qtiplot.diff
 )
@@ -106,4 +109,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/qwt.build h/qwt.build
--- w/qwt.build	2005-08-17 15:16:36.000000000 +0100
+++ h/qwt.build	2005-08-17 15:16:36.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=4.2.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/q
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME $PKGNAME-$VERSION
 fi
 
@@ -102,4 +105,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/qwtplot3d.build h/qwtplot3d.build
--- w/qwtplot3d.build	2005-08-17 13:25:49.000000000 +0100
+++ h/qwtplot3d.build	2005-08-17 13:25:49.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tgz
 TAR_OPTS=xzvf
 VERSION=0.2.5
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/q/qwtplot3d
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv $PKGNAME $PKGNAME-$VERSION
 fi
 
@@ -102,4 +105,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/rdesktop.build h/rdesktop.build
--- w/rdesktop.build	2005-05-09 21:26:29.000000000 +0100
+++ h/rdesktop.build	2005-05-09 21:26:29.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=rdesktop
 VERSION=1.4.1
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/r
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/recode.build h/recode.build
--- w/recode.build	2004-09-16 09:56:34.000000000 +0100
+++ h/recode.build	2004-09-16 09:56:34.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=recode
 VERSION=3.6
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/l
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -72,4 +83,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/ruby.build h/ruby.build
--- w/ruby.build	2005-07-26 09:29:26.000000000 +0100
+++ h/ruby.build	2005-07-26 09:29:26.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.8.2
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/r
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/scons.build h/scons.build
--- w/scons.build	2005-07-26 13:00:55.000000000 +0100
+++ h/scons.build	2005-07-26 13:00:55.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=0.96.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -103,4 +106,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/scorched3D.build h/scorched3D.build
--- w/scorched3D.build	2005-08-11 13:52:10.000000000 +0100
+++ h/scorched3D.build	2005-09-04 12:45:10.000000000 +0100
@@ -4,10 +4,12 @@
 PKGNAME=Scorched3D
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
-VERSION=39
+VERSION=39.1
 VERSIONEXTRA=-src
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
@@ -26,9 +28,9 @@
 #cd /tmp
 tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 mv scorched $PKGNAME-$VERSION
-(cd $PKGNAME-$VERSION
- patch < ../scorched3D.diff
-)
+#(cd $PKGNAME-$VERSION
+# patch -p0 < ../scorched3D.diff
+#)
 fi
 
 cd $PKGNAME-$VERSION
@@ -104,4 +106,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/scribus-templates.build h/scribus-templates.build
--- w/scribus-templates.build	2005-01-09 16:41:04.000000000 +0000
+++ h/scribus-templates.build	2005-01-09 16:41:04.000000000 +0000
@@ -4,19 +4,30 @@
 PKGNAME=scribus-templates
 PKGNAME1=scribus-temp-all
 VERSION=1.2.1
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME1-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME1-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -64,4 +75,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/scribus.build h/scribus.build
--- w/scribus.build	2005-07-05 20:50:29.000000000 +0100
+++ h/scribus.build	2005-07-05 20:50:29.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=scribus
 VERSION=1.2.2.1
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -63,4 +74,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/sdl_gfx.build h/sdl_gfx.build
--- w/sdl_gfx.build	2005-01-09 21:26:58.000000000 +0000
+++ h/sdl_gfx.build	2005-01-09 21:26:58.000000000 +0000
@@ -4,13 +4,23 @@
 PKGNAME=SDL_gfx
 VERSION=2.0.12
 VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 VERSIONPKG=$VERSION$VERSIONEXTRA
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 DATAFILE=$PKGDIR/$PKGNAME-data-$VERSION.tar.bz2
 
 rm -rf $PKG
@@ -19,7 +29,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSIONPKG.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSIONPKG$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -87,4 +97,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/sge.build h/sge.build
--- w/sge.build	2004-07-28 09:51:06.000000000 +0100
+++ h/sge.build	2004-07-28 09:51:06.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=sge
 VERSION=030809
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/x
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME$VERSION$PKG_EXT
 fi
 
 cd $PKGNAME$VERSION
@@ -56,4 +67,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/simgear.build h/simgear.build
--- w/simgear.build	2004-08-02 21:56:09.000000000 +0100
+++ h/simgear.build	2005-08-12 21:51:09.000000000 +0100
@@ -2,20 +2,32 @@
 #
 CWD=`pwd`
 PKGNAME=SimGear
-VERSION=0.3.6
+VERSION=0.3.8
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
+PKG_EXT=.tar.gz
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/f/Flightgear
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -53,18 +65,18 @@
 # customary to leave one space after the ':'.
 
           |-----handy-ruler------------------------------------------------------|
-plib: plib
-plib:
-plib: 
-plib:
-plib:
-plib:
-plib:
-plib:
-plib:
-plib:
-plib:
+SimGear: SimGear
+SimGear:
+SimGear: 
+SimGear:
+SimGear:
+SimGear:
+SimGear:
+SimGear:
+SimGear:
+SimGear:
+SimGear:
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/slib.build h/slib.build
--- w/slib.build	2005-09-26 09:29:05.000000000 +0100
+++ h/slib.build	2005-09-26 09:29:05.000000000 +0100
@@ -5,8 +5,11 @@
 PKG_EXT=.zip
 TAR_OPTS=xzvf
 VERSION=3a2
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
@@ -115,4 +118,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/slib2.build h/slib2.build
--- w/slib2.build	2005-09-26 09:25:28.000000000 +0100
+++ h/slib2.build	2005-09-26 09:25:28.000000000 +0100
@@ -5,8 +5,11 @@
 PKG_EXT=.zip
 TAR_OPTS=xzvf
 VERSION=2d6
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
@@ -110,4 +113,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/smpeg.build h/smpeg.build
--- w/smpeg.build	2005-09-16 11:06:36.000000000 +0100
+++ h/smpeg.build	2005-09-16 11:06:36.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=cvs
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
  ./autogen.sh
 )
@@ -101,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/squid.build h/squid.build
--- w/squid.build	2005-02-02 21:22:54.000000000 +0000
+++ h/squid.build	2005-02-02 21:22:54.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=squid
 VERSION=2.5.STABLE7
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -102,4 +113,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/subversion.build h/subversion.build
--- w/subversion.build	2005-07-25 14:54:45.000000000 +0100
+++ h/subversion.build	2005-07-25 14:54:45.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.2.1
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -22,7 +25,7 @@
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cd $PKGNAME-$VERSION
 ./autogen.sh
 cd ..
@@ -111,4 +114,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/supertux.build h/supertux.build
--- w/supertux.build	2005-07-12 09:47:37.000000000 +0100
+++ h/supertux.build	2005-07-12 09:47:37.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=0.1.3
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -98,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/swftools.build h/swftools.build
--- w/swftools.build	2005-06-16 22:03:48.000000000 +0100
+++ h/swftools.build	2005-09-12 21:17:36.000000000 +0100
@@ -2,20 +2,31 @@
 #
 CWD=`pwd`
 PKGNAME=swftools
-VERSION=0.6.3
+VERSION=0.7.0
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
 #patch -p1 < $CWD/swftools.diff
 )
@@ -65,4 +76,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/swh-plugins.build h/swh-plugins.build
--- w/swh-plugins.build	2005-03-01 21:03:34.000000000 +0000
+++ h/swh-plugins.build	2005-03-01 21:03:34.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=swh-plugins
 VERSION=0.4.13
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 #(cd $PKGNAME-$VERSION; patch < $PKGDIR/swh-0.4.12-sc4m.patch; ./autogen.sh)
 fi
 
@@ -63,4 +74,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/swig.build h/swig.build
--- w/swig.build	2005-03-11 14:12:03.000000000 +0000
+++ h/swig.build	2005-03-11 14:12:03.000000000 +0000
@@ -4,20 +4,31 @@
 PKGNAME=swig
 PKGNAME1=SWIG
 VERSION=1.3.24
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 EXTRAVERSION=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/s
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME1-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 fi
 
@@ -93,4 +104,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/t1lib.build h/t1lib.build
--- w/t1lib.build	2005-08-18 09:36:51.000000000 +0100
+++ h/t1lib.build	2005-08-18 09:36:51.000000000 +0100
@@ -5,15 +5,18 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=5.1.0
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/t
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -23,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
  patch -p0 < ../t1lib.diff
 )
@@ -127,4 +130,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/template.build h/template.build
--- w/template.build	2005-07-30 21:48:39.000000000 +0100
+++ h/template.build	2005-09-14 21:32:16.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/transcode.build h/transcode.build
--- w/transcode.build	2005-01-07 14:15:52.000000000 +0000
+++ h/transcode.build	2005-01-07 14:15:52.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=transcode
 VERSION=0.6.14
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/t
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 
 #cd $PKGNAME-$VERSION
 #patch -p1 < ../transcode-0.6.12-fix_dv-1.patch
@@ -124,4 +135,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/units.build h/units.build
--- w/units.build	2005-08-05 13:47:43.000000000 +0100
+++ h/units.build	2005-08-05 13:47:43.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=1.85
+VERSIONEXTRA=
 ARCH=i486
 BUILD=1
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/gnu/units
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/vcdimager.build h/vcdimager.build
--- w/vcdimager.build	2005-01-06 15:27:28.000000000 +0000
+++ h/vcdimager.build	2005-01-06 15:27:28.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=vcdimager
 VERSION=cvs
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/v
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cd $PKGNAME-$VERSION
 ./autogen.sh
 cd ..
@@ -97,4 +108,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/vsound.build h/vsound.build
--- w/vsound.build	2005-04-26 21:58:58.000000000 +0100
+++ h/vsound.build	2005-04-26 21:58:58.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=vsound
 VERSION=0.6
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/v
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 cd $PKGNAME-$VERSION
 #zcat $PKGDIR/${PKGNAME}_0.5-5.diff.gz | patch -p1
 cd ..
@@ -89,4 +100,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/webcleaner.build h/webcleaner.build
--- w/webcleaner.build	2004-10-16 22:33:30.000000000 +0100
+++ h/webcleaner.build	2004-10-16 22:33:30.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=webcleaner
 VERSION=2.20
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/w
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -84,4 +95,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/wormux.build h/wormux.build
--- w/wormux.build	2005-02-27 22:11:30.000000000 +0000
+++ h/wormux.build	2005-02-27 22:11:30.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=wormux
 VERSION=0.5.0
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/w
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-src-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-src-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -90,4 +101,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/wxGTK.build h/wxGTK.build
--- w/wxGTK.build	2004-09-01 22:02:15.000000000 +0100
+++ h/wxGTK.build	2005-09-09 20:32:58.000000000 +0100
@@ -2,20 +2,31 @@
 #
 CWD=`pwd`
 PKGNAME=wxGTK
-VERSION=2.4.2
+VERSION=2.6.1
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/w/wxWindows
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -31,6 +42,9 @@
 
 make install prefix=$PKG/usr
 
+rm $PKG/usr/bin/wx-config
+ln -s /usr/lib/wx/config/gtk2-ansi-release-2.6 $PKG/usr/bin/wx-config
+
 # compress man pages
 find $PKG/usr/man -exec gzip {} \;
 
@@ -62,4 +76,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/wxmaxima.build h/wxmaxima.build
--- w/wxmaxima.build	2005-01-19 09:01:30.000000000 +0000
+++ h/wxmaxima.build	2005-01-19 09:01:30.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=wxMaxima
 VERSION=0.5.3
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/w
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -91,4 +102,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chown -R root.bin $PKG/usr/bin; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/xawtv.build h/xawtv.build
--- w/xawtv.build	2005-01-07 22:33:06.000000000 +0000
+++ h/xawtv.build	2005-01-07 22:33:06.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=xawtv
 VERSION=3.94
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=2
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/x
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION; patch -p1 < ../xawtv.diff)
 fi
 
@@ -88,4 +99,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; chmod 4755 $PKG/usr/bin/v4l-conf; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; chmod 4755 $PKG/usr/bin/v4l-conf; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/xephem.build h/xephem.build
--- w/xephem.build	2005-01-14 22:57:45.000000000 +0000
+++ h/xephem.build	2005-01-14 22:57:45.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=xephem
 VERSION=3.6.3
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/x
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -83,4 +94,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/xsane.build h/xsane.build
--- w/xsane.build	2005-01-23 19:37:50.000000000 +0000
+++ h/xsane.build	2005-01-23 19:37:50.000000000 +0000
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=xsane
 VERSION=0.97
+VERSIONEXTRA=
+PKG_EXT=.tar.gz
+TAR_OPTS=xzvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/x
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xzvf $PKGDIR/$PKGNAME-$VERSION.tar.gz
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
@@ -83,4 +94,4 @@
 fi
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
diff -ur w/xscreensaver.build h/xscreensaver.build
--- w/xscreensaver.build	2005-06-23 20:53:39.000000000 +0100
+++ h/xscreensaver.build	2005-06-23 20:53:39.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.gz
 TAR_OPTS=xzvf
 VERSION=4.22
+VERSIONEXTRA=
 ARCH=i486
 BUILD=3
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/x
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 (cd $PKGNAME-$VERSION
 #patch -p1 < $CWD/xscreensaver.setuid.diff
 )
diff -ur w/xscreensaver1.build h/xscreensaver1.build
--- w/xscreensaver1.build	2005-06-17 20:58:04.000000000 +0100
+++ h/xscreensaver1.build	2005-06-17 20:58:04.000000000 +0100
@@ -5,6 +5,7 @@
 PKG_EXT=.tar.bz2
 TAR_OPTS=xjvf
 VERSION=4.19
+VERSIONEXTRA=
 ARCH=i486
 BUILD=3
 USERNAME=`id -un`
@@ -13,9 +14,9 @@
 PKGDIR=~/tmp/new/x
 PKG=/tmp/package-$PKGNAME
 
-if [ ! -f $PKGDIR/$PKGNAME-$VERSION$PKG_EXT ]
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
 then
-  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+  echo Can\'t find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
   exit
 fi
 
@@ -25,7 +26,7 @@
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$PKG_EXT
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION
diff -ur w/xvidcore.build h/xvidcore.build
--- w/xvidcore.build	2004-07-23 21:54:00.000000000 +0100
+++ h/xvidcore.build	2004-07-23 21:54:00.000000000 +0100
@@ -3,19 +3,30 @@
 CWD=`pwd`
 PKGNAME=xvidcore
 VERSION=1.0.1
+VERSIONEXTRA=
+PKG_EXT=.tar.bz2
+TAR_OPTS=xjvf
 ARCH=i486
 BUILD=1
+USERNAME=`id -un`
+GROUP=`id -gn`
 
 PKGDIR=~/tmp/new/x
 PKG=/tmp/package-$PKGNAME
 
+if [ ! -f $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT ]
+then
+  echo Can't find $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
+  exit
+fi
+
 rm -rf $PKG
 mkdir -p $PKG
 
 if [ ! -d $PKGNAME-$VERSION ]
 then 
 #cd /tmp
-tar xjvf $PKGDIR/$PKGNAME-$VERSION.tar.bz2
+tar $TAR_OPTS $PKGDIR/$PKGNAME-$VERSION$VERSIONEXTRA$PKG_EXT
 fi
 
 cd $PKGNAME-$VERSION/build/generic
@@ -60,4 +71,4 @@
 EOF
 
 cd $PKG
-su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R simon.users $PKG"
+su -c "chown -R root.root *; makepkg $PKGNAME-$VERSION-$ARCH-$BUILD.tgz; chown -R $USERNAME.$GROUP $PKG"
