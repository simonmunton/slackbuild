--- calligra-25.12.0/filters/karbon/pdf/SvgOutputDev.cpp.orig	2025-12-04 02:39:20.000000000 +0000
+++ calligra-25.12.0/filters/karbon/pdf/SvgOutputDev.cpp	2026-01-07 14:16:45.462107743 +0000
@@ -492,7 +492,11 @@
     Q_UNUSED(interpolate)
     Q_UNUSED(inlineImg)
     ImageStream *imgStr = new ImageStream(str, width, colorMap->getNumPixelComps(), colorMap->getBits());
+#if POPPLER_VERSION_MACRO < QT_VERSION_CHECK(26, 1, 0)
     imgStr->reset();
+#else
+    imgStr->rewind();
+#endif
 
     unsigned int *dest = nullptr;
     unsigned char *buffer = new unsigned char[width * height * 4];
